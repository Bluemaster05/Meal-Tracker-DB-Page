@page
@model Meal_Planner.Pages.RecipeIngredient.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RecipeIngredient</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RecipeIngredient.Amount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RecipeIngredient.Amount)
        </dd>
        <dt class="col-sm-2">
            Ingredient
        </dt>
        <dd class="col-sm-10">
            @Model.RecipeIngredient.Ingredient?.Name
        </dd>

        <dt class="col-sm-2">
            Meal Item
        </dt>
        <dd class="col-sm-10">
            @Model.RecipeIngredient.Item?.Name
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" name="itemId" value="@Model.RecipeIngredient.ItemId" />
        <input type="hidden" name="ingredientId" value="@Model.RecipeIngredient.IngredientId" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a class="btn btn-secondary" asp-page="./Index">Back to List</a>
    </form>
</div>
